sources = ['ssl.c', 'ssl.h']
ssl_lib_static = static_library('ssl',
                                sources,
                                dependencies: [helpers_dep, crypto, openssl]
                                )